import { createAsyncThunk } from "@reduxjs/toolkit";

// export const addProductToCart = createAsyncThunk(
//   "user/get",
//   async (product: CartPayload) => {
//     const { data } = await ADD_PRODUCT_TO_CART(product);
//     return data;
//   }
// );
